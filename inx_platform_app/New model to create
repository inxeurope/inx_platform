New model to create:
modelname: BomHeader - contains the header of the BOM, it is possible to have 2 or more boms per each Product number
---------
FK -> Product
Alt Bom (a single digit integer)
Header Base Quantity (decimal number, 2 decimal digits)
Header Base Quantity UoM (string max_length=5)
add UNIQUE constraint - FK_Product + AltBom

modelname: BomComponent - lists all possible BOM components
------------
FK -> BomHeader
Componenet Material (string max_length=30)
Componenet Base UoM (string max_length=5)
add UNIQUE constraint - FK_BomHeader + ComponentMaterial + ItemNumber

model:name Bom - links BomHeader and BomComponent, adds other fields
---
FK -> BomHeader
FK -> BomComponent
ItemNumber (integer)
Component Quantity (decimal, 2 decimal digits)
Component UoM in Bom (string max_length=5)
Price Unit (integer)
Standard Price per Unit (decimal, 2 decimal digits)

Process description:
- open the Excel file with Pandas, make a dataframe
- remove lines where Finished Material is empty
- filter out rows where Plant column == 8800
- find which Finished Material is not in the Product model's records
    - add them and set the is_new value
- find unique values of Component Material, Component Material Desc, Componenet Base UoM
    - add them or edit them in the BomComponent model
- per each distinct combination of Finished Material, Finished Material Desc, Alt Bom of the dataframe
    - find the pk of the Product using the Finished Material
    - add or edit it in the BomHeader model using the values of HEader Base Qty, Header Base UoM
    - for that combination of Finished Material, Finished Material Desc, Alt Bom of the dataframe
        - get the lines from the dataframe
        - (remove the lines from the Bom)
        - find the component pk in the BomComponent model using the Component Material column in the dataframe
        - add the record in the Bom model using the pk to the BomComponent model (FK) using the additional values of ItemNumber, Component Quantity, Component UoM in Bom, Price Unit, Standard Price per Unit
